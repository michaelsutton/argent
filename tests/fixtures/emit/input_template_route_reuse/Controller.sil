pragma silverscript ^0.1.0;

// Generated by argentc. Do not edit by hand.

contract Controller(
    byte[32] gen__init_peer_template,
    int init_tick
) {
    // :: state layouts
    struct PeerState {
        // :: generated fields

        // :: user declared fields
        int count;
    }

    // :: route templates
    byte[32] gen__peer_template = gen__init_peer_template;

    // :: state fields: Controller
    int tick = init_tick;

    // :: entrypoints
    entrypoint function step(int gen__peer_prefix_len, int gen__peer_suffix_len) {
        // :: cov inputs
        byte[32] gen__cov_id = OpInputCovenantId(this.activeInputIndex);
        require(OpCovInputCount(gen__cov_id) == 2);
        require(OpCovInputIdx(gen__cov_id, 0) == this.activeInputIndex);
        int gen__peer_input_idx = OpCovInputIdx(gen__cov_id, 1); // input Peer at cov[1]
        PeerState peer = readInputStateWithTemplate(
            gen__peer_input_idx,
            gen__peer_prefix_len,
            gen__peer_suffix_len,
            gen__peer_template
        );

        // :: auth outputs
        require(OpAuthOutputCount(this.activeInputIndex) == 1);
        int gen__peer_output_idx = OpAuthOutputIdx(this.activeInputIndex, 0); // output peer: Peer

        PeerState next_peer = {
            // :: generated fields

            // :: user declared fields
            count: peer.count + 1,
        };
        // :: become Peer
        validateOutputStateWithInputTemplate(
            gen__peer_output_idx,
            next_peer,
            gen__peer_input_idx,
            gen__peer_prefix_len,
            gen__peer_suffix_len,
            gen__peer_template
        );
    }

}
