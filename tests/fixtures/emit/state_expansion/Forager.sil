pragma silverscript ^0.1.0;

// Generated by argentc. Do not edit by hand.

contract Forager(
    byte[32] init_strategy,
    int init_energy
) {
    // :: state layouts

    // :: route templates

    // :: state fields: Forager
    byte[32] strategy = init_strategy;
    int energy = init_energy;

    // :: entrypoints
    // :: leader entry (1:N)
    entrypoint function hold(byte[8] gen__strategy_forager_strategy_preimage) {
        // :: expanded state
        require(blake2b(gen__strategy_forager_strategy_preimage) == strategy);
        int gen__strategy_hunger = OpBin2Num(gen__strategy_forager_strategy_preimage.slice(0, 8));

        // :: auth outputs
        require(OpAuthOutputCount(this.activeInputIndex) == 1);
        int gen__next_output_idx = OpAuthOutputIdx(this.activeInputIndex, 0); // emits one Forager

        require(energy > 0);
        State next_state = {
            // :: generated fields

            // :: user declared fields
            strategy: blake2b(byte[8](gen__strategy_hunger + 1)),
            energy: energy - 1,
        };
        // :: become Forager
        validateOutputState(gen__next_output_idx, next_state);
    }

}
