pragma silverscript ^0.1.0;

// Generated by argentc. Do not edit by hand.
// This is plain Silverscript: no covenant macros are used.

contract StonesSettle(
    byte[32] init_template_league,
    byte[32] init_template_player,
    byte[32] init_template_stones_game,
    byte[32] init_template_stones_settle,
    byte[32] init_player_a,
    byte[32] init_player_b,
    int init_status
) {
    // Shared constants imported from Argent modules.
    int constant SIDE_A = 0;
    int constant SIDE_B = 1;
    int constant LIVE = 0;
    int constant A_WIN = 1;
    int constant B_WIN = 2;

    // User state structs imported from Argent modules.
    struct LeagueState {
        byte[32] admin;
        int default_pile;
        int default_max_take;
    }
    struct PlayerState {
        byte[32] owner;
        byte[32] player_id;
        int open_games;
        int games;
        int wins;
        int losses;
    }
    struct GameState {
        byte[32] player_a;
        byte[32] player_b;
        int pile;
        int max_take;
        int turn;
    }
    struct SettleState {
        byte[32] player_a;
        byte[32] player_b;
        int status;
    }

    // Shared helper functions imported from Argent modules.
    function player_ref(byte[32] owner, byte[32] player_id) : byte[32] {
        return blake2b(owner + player_id);
    }
    function side_player(int side, byte[32] player_a, byte[32] player_b) : byte[32] {
        if (side == SIDE_A) {
            return player_a;
        }

        require(side == SIDE_B);
        return player_b;
    }
    function other_side(int side) : int {
        if (side == SIDE_A) {
            return SIDE_B;
        }

        require(side == SIDE_B);
        return SIDE_A;
    }
    function win_status(int side) : int {
        if (side == SIDE_A) {
            return A_WIN;
        }

        require(side == SIDE_B);
        return B_WIN;
    }

    // Hidden template capability table generated from the Argent app manifest.
    byte[32] template_league = init_template_league;
    byte[32] template_player = init_template_player;
    byte[32] template_stones_game = init_template_stones_game;
    byte[32] template_stones_settle = init_template_stones_settle;

    // User state owned by Argent actor `StonesSettle`.
    byte[32] player_a = init_player_a;
    byte[32] player_b = init_player_b;
    int status = init_status;

    // Full state layouts used for generated cross-template reads and writes.
    struct ArgentLeagueState {
        byte[32] template_league;
        byte[32] template_player;
        byte[32] template_stones_game;
        byte[32] template_stones_settle;
        byte[32] admin;
        int default_pile;
        int default_max_take;
    }
    struct ArgentPlayerState {
        byte[32] template_league;
        byte[32] template_player;
        byte[32] template_stones_game;
        byte[32] template_stones_settle;
        byte[32] owner;
        byte[32] player_id;
        int open_games;
        int games;
        int wins;
        int losses;
    }
    struct ArgentStonesGameState {
        byte[32] template_league;
        byte[32] template_player;
        byte[32] template_stones_game;
        byte[32] template_stones_settle;
        byte[32] player_a;
        byte[32] player_b;
        int pile;
        int max_take;
        int turn;
    }
    struct ArgentStonesSettleState {
        byte[32] template_league;
        byte[32] template_player;
        byte[32] template_stones_game;
        byte[32] template_stones_settle;
        byte[32] player_a;
        byte[32] player_b;
        int status;
    }

    entrypoint function settle(byte[] player_prefix, byte[] player_suffix) {
        int player_prefix_len = player_prefix.length;
        int player_suffix_len = player_suffix.length;

        byte[32] cov_id = OpInputCovenantId(this.activeInputIndex);
        require(OpCovInputCount(cov_id) == 3);
        require(OpCovInputIdx(cov_id, 0) == this.activeInputIndex);
        int player_a_in_input_idx = OpCovInputIdx(cov_id, 1); // Argent input Player at cov[1]
        ArgentPlayerState player_a_in = readInputStateWithTemplate(player_a_in_input_idx, player_prefix_len, player_suffix_len, template_player);
        int player_b_in_input_idx = OpCovInputIdx(cov_id, 2); // Argent input Player at cov[2]
        ArgentPlayerState player_b_in = readInputStateWithTemplate(player_b_in_input_idx, player_prefix_len, player_suffix_len, template_player);
        require(OpAuthOutputCount(this.activeInputIndex) == 2);
        int player_a_out_output_idx = OpAuthOutputIdx(this.activeInputIndex, 0); // Argent output player_a_out: Player
        int player_b_out_output_idx = OpAuthOutputIdx(this.activeInputIndex, 1); // Argent output player_b_out: Player

        // Argent become routes extracted for the next lowering pass.
        // become player_a_out <- Player(next_player_a);
        // validateOutputStateWithTemplate(player_a_out_output_idx, <ArgentPlayerState>, player_prefix, player_suffix, template_player);
        // become player_b_out <- Player(next_player_b);
        // validateOutputStateWithTemplate(player_b_out_output_idx, <ArgentPlayerState>, player_prefix, player_suffix, template_player);
        // Current actor `StonesSettle` copies template_* fields into every generated successor state.

        // TODO: lower Argent body.
        // Raw Argent body is retained in the AST for the next compiler pass.
        require(1 == 1);
    }

}
